[package]
name = "mtga-rag3bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
rag3weaver = { path = "../../../extension/rag3weaver", default-features = false, features = ["daemon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
