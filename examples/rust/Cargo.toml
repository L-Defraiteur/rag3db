[package]
name = "rag3db-rust-example"
version = "0.1.0"
edition = "2021"

[dependencies]
rag3db = { path = "../../tools/rust_api" }
arrow = { version = "55", optional = true, default-features = false, features = [] }

[features]
arrow = ["rag3db/arrow", "dep:arrow"]
